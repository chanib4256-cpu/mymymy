{
  "name": "my-downloader-proxy",
  "version": "1.0.0",
  "main": "api/index.js",
  "scripts": {
    "postinstall": "apt-get update && apt-get install -y ffmpeg python3-pip && pip3 install -U yt-dlp"
  },
  "dependencies": {
    "axios": "^1.1.0"
  }
}
